# Sistema Infra

Workflow operacional de pedidos — demo frontend (React + Vite + TypeScript).

## Login demo

- **Admin:** `adm` / `adm123`

## Desenvolvimento

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

## Deploy (Vercel)

Conecte o repositório e use o preset Vite. Build command: `npm run build`. Output: `dist`.
